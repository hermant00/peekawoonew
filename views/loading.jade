extends layout

block content
    body.container.error-bg
        .head.peekawoo-error-logo2
            input(type="hidden",id="user",value=JSON.stringify(user))
            
            .container.loading-image
                    .container
                    p.text-loading Please wait while the chat room loads...
        script( src="/socket.io/socket.io.js")
        script( src="/javascripts/jquery-2.0.3.min.js")
        script(type='text/javascript', src="/javascripts/loading.js")
        script(language='JavaScript', type='text/javascript', src='javascripts/Audio-HTML5.js')
        script
            $(document).unbind('keydown').bind('keydown', function (event) {
                if (event.keyCode === 8 || event.keyCode === 116 || event.keyCode === 115) {
                    event.preventDefault();
                }
            });
            var _gaq = _gaq || [];
            _gaq.push(['_setAccount', 'UA-43851834-1']);
            _gaq.push(['_trackPageview']);
            (function() {
                var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
                ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
                var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
            })();
            
        script
            window.location.hash="no-back-button";
            window.location.hash="Again-no-back-button";//for google chrome
            window.onhashchange=function(){window.location.hash="no-back-button";}